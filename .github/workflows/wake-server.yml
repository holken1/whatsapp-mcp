# .github/workflows/keep-alive.yml

name: Wake MCP server in time
on:
  schedule:
    - cron: '5 5 * * *'   # 07:05 CEST
  workflow_dispatch:          # Möjlighet att trigga manuellt

jobs:
  warmup:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Run warmup script to wake MCP server
        run: ./warmup.sh
